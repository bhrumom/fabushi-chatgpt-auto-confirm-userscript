# 项目章程

问题：自动确认脚本在部分错误分支把任务置为 `blocked`，UI 显示“需要处理”，调度器因此停止。

目标：自动运行开启时，将任何 blocked 过渡转换为可持续的 fresh-session resend；升级后历史 blocked 任务也自动恢复。成功定义：运行时与启动迁移均不会让可运行任务停在 blocked，并有回归测试、PR CI、main 回读和 Release 证据。
