# 架构与实现策略

集中在 `state(task, value, message)` 的 blocked 入口进行自动恢复；新增 `queueBlockedFreshRetry` 复用 `clearDispatchIntent`，把任务回到 queued。`recoverPersistedBlockedTasks` 负责升级迁移历史 blocked。特定直接赋值 blocked 的 review repair 分支改为调用同一恢复函数。已有发送歧义与 bound-conversation 检查保持不变，在真正 blocked 前仍优先检查当前轮会话。
