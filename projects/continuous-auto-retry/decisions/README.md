# Decisions

No separate ADR required: this change refines the existing recovery state machine and is reversible without a new external protocol. If retry identity or persistence protocol changes across components, add an ADR.
